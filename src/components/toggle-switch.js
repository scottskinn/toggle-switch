import React from 'react'; 


const ToggleSwitch = (props) => {
  
  return ( 
    
    <button className="Toggle" onClick={props.onClick}>New Jokes</button> 
    
  ); 
  
} 

export default ToggleSwitch;